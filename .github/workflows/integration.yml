# name: Integartion

# on:
#     push:
#         branches:
#         - main
#     pull_request:
#         branches:
#         - main

# jobs:
#     build: 
#         runs-on: ubuntu-latest

#         strategy:
#             matrix:
#                 node-version: [14.x, 16.x, 18.x]

#         steps:
#         - uses: actions/checkout@v4
#         - name: Use Node.js ${{ matrix.node-version }}
#           uses: actions/setup-node@v3
#           with:
#             node-version: ${{ matrix.node-version }}
#             cache: 'npm'
#         - run: npm run build --id-present
#         - run: npm test
# name: Integration

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main

# env:
#   NEXT_PUBLIC_STACK_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_STACK_PROJECT_ID }}
#   DATABASE_URL: ${{ secrets.DATABASE_URL }}

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [20.x]

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js ${{ matrix.node-version }}
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ matrix.node-version }}
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm install

#       - name: Build project
#         run: npm run build


name: CI/CD Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      NEXT_PUBLIC_STACK_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_STACK_PROJECT_ID }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      # ✅ Step 1: Create .env.local
      - name: Create .env.local
        run: |
          echo "DATABASE_URL=${DATABASE_URL}" >> .env.local
          echo "NEXT_PUBLIC_STACK_PROJECT_ID=${NEXT_PUBLIC_STACK_PROJECT_ID}" >> .env.local

      # ✅ Step 2: Export env variables (optional, extra safety)
      - name: Export env variables
        run: |
          export DATABASE_URL=${DATABASE_URL}
          export NEXT_PUBLIC_STACK_PROJECT_ID=${NEXT_PUBLIC_STACK_PROJECT_ID}

      # ✅ Step 3: Prisma commands
      - run: npx prisma db push
      - run: npx prisma generate
      - run: npm run seed

      # ✅ Step 4: Build Next.js
      - run: npm run build

